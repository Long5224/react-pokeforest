import React from 'react'
import PropTypes from 'prop-types'

function Card(props) {
    return (
        <div>
            <h1>fdsfs</h1>
        </div>
    )
}

Card.propTypes = {
}

export default Card

